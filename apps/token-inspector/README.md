# token-inspector

Planned Figma plugin. Implementation and detailed requirements are not defined yet.

Use React, TypeScript, Vite, and the shared shadcn components and neutral theme from `@figma-tools/ui`. This folder is a placeholder, not a runnable workspace package. Each implemented plugin will own its manifest, UI, Figma sandbox code, and build output.
